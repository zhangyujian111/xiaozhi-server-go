# 设备 API Key 加密存储文件
# 此文件由 xiaozhi-server-go 首次启动时自动生成
# 格式: hex(AES-256-GCM(nonce || encrypted_apiKey))
# 密钥派生: HKDF(efuseMAC, salt="xiaozhi-key-v1", info=deviceID, len=32)
#
# 请勿手动编辑此文件
# 如需重新注册设备，删除此文件后重启服务即可